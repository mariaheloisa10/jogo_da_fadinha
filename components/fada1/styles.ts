import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  container:{
    // width:30,
    // height:30,
    // backgroundColor: '#fff',
    // borderRadius: 25,
    position: 'absolute',
  },
})